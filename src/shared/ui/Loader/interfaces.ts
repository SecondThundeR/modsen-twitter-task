export interface LoaderProps {
  text?: string;
  inline?: boolean;
  invert?: boolean;
}

export interface LoaderWrapperStyleProps {
  $disablePadding: boolean;
}

export interface LoaderStyleProps {
  $inline: boolean;
}
