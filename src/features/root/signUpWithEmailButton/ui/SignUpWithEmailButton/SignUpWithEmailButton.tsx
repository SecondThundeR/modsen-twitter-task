import { memo } from "react";

import { Button } from "@/shared/ui";

export const SignUpWithEmailButton = memo(function SignUpWithEmailButton() {
  // TODO: Add Email Sign In feature
  return <Button text="Sign up with email" />;
});
